# Public data directory
# JSON files are generated by the Python data pipeline.
# Run data/scripts/generate_web_data.py to populate this directory.
#
# Expected files:
#   climate_data.json  — daily records (date, temp_max, temp_min, ...)
#   metrics.json       — annual metrics keyed by year
#   summary.json       — headline stats and trend data
